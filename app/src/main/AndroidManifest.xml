<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ccmcteam.ccmcteam">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_app_final"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity
            android:name=".activities.AddIngredient"
            android:theme="@style/AppTheme">

        </activity>
        <!-- sign in up activity -->
        <activity
            android:name=".activities.signInUp.BeginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.signInUp.RegisterActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.signInUp.LoginActivity"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name=".nguyenlieu.add_ingredient_activity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.RecipeFormInsertActivity"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name=".activities.update_user_infomation"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name=".activities.IntroActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".splashscreen.splashscreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.CookActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>